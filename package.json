{
  "name": "telegram-trader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local-first Telegram to MetaTrader 5 automated trading signal gateway",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "tsx watch --include .env src/main.ts | pino-pretty --colorize --translateTime SYS:HH:MM:ss --ignore pid,hostname,service",
    "start": "node dist/src/main.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "tsx src/database/migrate.ts",
    "telegram:login": "tsx src/scripts/telegram-login.ts",
    "telegram:list-chats": "tsx src/scripts/telegram-list-chats.ts",
    "telegram:resolve-phone": "tsx src/scripts/telegram-resolve-phone.ts"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.3.0",
    "@mtcute/node": "^0.31.0",
    "better-sqlite3": "^12.2.0",
    "cross-spawn": "^7.0.6",
    "decimal.js": "^10.6.0",
    "dotenv": "^17.2.1",
    "fastify": "^5.5.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/cross-spawn": "^6.0.6",
    "@types/node": "^24.3.0",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
